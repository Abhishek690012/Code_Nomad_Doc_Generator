#Temproray function to use just to prevent import error

def answer(question):
    return {
        "answer": f"This is a mock answer to: '{question}'",
        "source_file": "mock_file.py",
        "source_function": "mock_function"
    }